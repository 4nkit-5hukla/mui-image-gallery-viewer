import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const TransitionContainer = styled(Box)({
  position: "relative",
  width: "100%",
  height: "100%",
});

export const TransitionContent = styled(Box)({
  width: "100%",
  height: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
});
